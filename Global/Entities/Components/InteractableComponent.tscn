[gd_scene format=3 uid="uid://bkbvrk6km27sf"]

[ext_resource type="Script" uid="uid://q77dpb3rmdu8" path="res://Global/Entities/Components/interactable_component.gd" id="1_fydwd"]
[ext_resource type="Texture2D" uid="uid://cf7hvkou11u6t" path="res://Assets/Art/UI/InteractionUI_Borderless.aseprite" id="2_11sxa"]

[sub_resource type="CircleShape2D" id="CircleShape2D_fydwd"]
radius = 37.054016

[sub_resource type="Shader" id="Shader_11sxa"]
code = "shader_type canvas_item;

// === User Controls ===

// Color of the outline
uniform vec4 outline_color    : source_color = vec4(1.0);
// How far to search around a pixel for solid neighbors (in texels)
uniform float outline_size    : hint_range(0.0, 16.0, 0.1) = 1.0;
// Minimum alpha for a pixel to be considered solid
uniform float alpha_threshold : hint_range(0.0, 1.0, 0.01) = 0.0;
// Number of directions to sample from (max 8)
uniform int outline_samples   : hint_range(4, 8) = 4;
// Color to blend with solid pixels (alpha is blend strength)
uniform vec4 shade_color      : source_color = vec4(1.0, 1.0, 1.0, .5); 

// === Outline Sampling Directions ===

const vec2[8] DIRS = vec2[8]
(vec2(1.0, 0.0), vec2(0.0, 1.0),  vec2(-1.0, 0.0),  vec2(0.0, -1.0),
 vec2(1.0, 1.0), vec2(-1.0, 1.0), vec2(-1.0, -1.0), vec2(1.0, -1.0));

// === Main Fragment ===

void fragment() {
	vec4 tex = texture(TEXTURE, UV);
	float alpha = tex.a;
	
	 // Skip fully transparent pixel with no outline
	if (alpha <= 0.0 && outline_size <= 0.0) discard;

	bool solid = alpha > alpha_threshold;
	bool semi  = (alpha > 0.0) && !solid;
	vec4 out_color = tex;

	// Blend solid pixels toward the shade_color based on its alpha
	if (solid && shade_color.a > 0.0) {
		out_color.rgb = mix(tex.rgb, shade_color.rgb, shade_color.a);
		out_color.a   = alpha;
	}

	bool found = false;

	// If not solid, check surrounding pixels for solid neighbors
	if (!solid && outline_size > 0.0) {
		vec2 offset = TEXTURE_PIXEL_SIZE * outline_size;
		for (int i = 0; i < outline_samples; i++) {
			if (texture(TEXTURE, UV + DIRS[i] * offset).a > alpha_threshold) {
				found = true;
				break;
			}
		}
	}

	// Set final pixel color
	if      (solid) COLOR = out_color;
	else if (found) COLOR = outline_color;
	else if (semi)  COLOR = tex;
	else            discard;
}

//void light() {
//	// Called for every pixel for every light affecting the CanvasItem.
//	// Uncomment to replace the default light processing function with this one.
//}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_oltsx"]
resource_local_to_scene = true
shader = SubResource("Shader_11sxa")
shader_parameter/outline_color = Color(0.105882354, 0.12156863, 0.12941177, 1)
shader_parameter/outline_size = 1.0000000149012
shader_parameter/alpha_threshold = 0.0
shader_parameter/outline_samples = 4
shader_parameter/shade_color = Color(0.24705882, 0.24705882, 0.24705882, 0.57254905)

[node name="InteractableComponent" type="Area2D" unique_id=71018077 node_paths=PackedStringArray("interact_texture")]
collision_layer = 16
collision_mask = 0
script = ExtResource("1_fydwd")
interact_name = "Wyrd Dialogue"
interact_texture = NodePath("InteractionGraphic")
border_color_on = Color(0.59607846, 0.27058825, 0.69411767, 1)
border_color_off = Color(0.50980395, 0.5254902, 0.4862745, 1)

[node name="CollisionShape2D" type="CollisionShape2D" parent="." unique_id=1529183704]
shape = SubResource("CircleShape2D_fydwd")
debug_color = Color(0.32941177, 0.60784316, 0.4392157, 0.41960785)

[node name="InteractionGraphic" type="TextureRect" parent="." unique_id=1845894608]
material = SubResource("ShaderMaterial_oltsx")
anchors_preset = 7
anchor_left = 0.5
anchor_top = 1.0
anchor_right = 0.5
anchor_bottom = 1.0
offset_left = -11.0
offset_top = -40.0
offset_right = -11.0
offset_bottom = -40.0
grow_horizontal = 2
grow_vertical = 0
pivot_offset = Vector2(11, 18)
texture = ExtResource("2_11sxa")
expand_mode = 5
stretch_mode = 2
metadata/_edit_use_anchors_ = true
